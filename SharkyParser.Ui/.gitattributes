# Force GitHub to include the SharkyParser submodule in language statistics
SharkyParser linguist-detectable=true

# Mark C# files as detectable (though they are in a submodule)
*.cs linguist-detectable=true

# Optional: Ignore some vendor/build folders if they show up
node_modules/* linguist-vendored
dotnet/* linguist-vendored
dist/* linguist-vendored
